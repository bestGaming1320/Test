name: 'Hello World'
description: 'Greet someone and record the time'
on: push
inputs:
  fileN:  # file name and path
    description: 'file name and path'
    required: true
    default: 'World'
#outputs:
#  time: # id of output
#    description: 'The time we greeted you'
runs:
  using: 'node12' 
  main: 'index.js'
